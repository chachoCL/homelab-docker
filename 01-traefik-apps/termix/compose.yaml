services:
  termix:
    image: ghcr.io/lukegus/termix:latest
    container_name: termix
    restart: unless-stopped
    networks:
      - proxy
    volumes:
      - ./termix-data:/app/data
    environment:
      PORT: "8080"

networks:
  proxy:
    external: true
