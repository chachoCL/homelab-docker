services:
  uptime-kuma:
    image: louislam/uptime-kuma:2.2.1
    container_name: uptimekuma
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - proxy
  #  labels:
  #    - sablier.enable=true
  #    - sablier.group=uptimekuma

 #   ports:
 #     - 3001:3001

networks:
  proxy:
    external: true
